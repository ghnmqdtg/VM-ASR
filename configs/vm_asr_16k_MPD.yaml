MODEL:
  TYPE: 'VM_ASR'
  NAME: 'DualStreamVMUNet'
  VSSM:
    DEPTHS: [2, 2, 2, 2]
    DIMS: 32
    SSM_D_STATE: 16
    SSM_DT_RANK: "auto"
    SSM_RATIO: 1.0
    SSM_CONV: 3
    SSM_CONV_BIAS: false
    SSM_FORWARDTYPE: "v04_ondwconv3"
    MLP_RATIO: 4.0
    DROP_PATH_RATE: 0.2
    DOWNSAMPLE: "v3"
    UPSAMPLE: "v2"
    PATCHEMBED: "v2"
    NORM_LAYER: "ln"
    OUTPUT: "v1"
    CONCAT_SKIP: True
    SKIP_CONNECT_PATCH: True
    DROP_LAST_ENCODER: True

TRAIN:
  EARLY_STOPPING: 10
  LOSSES:
    STFT_LOSS:
      SC_FACTOR: 1.0
      MAG_FACTOR: 1.0
      PHASE_FACTOR: 0.0

  LOW_FREQ_REPLACEMENT: True
  CLIP_GRAD:
    ENABLE: True
    MAX_NORM: 1e-2
  GRADIENT_PENALTY:
    ENABLE: False
  ADVERSARIAL:
    ENABLE: True
    DISCRIMINATORS: ["mpd"]
    GAN_LOSS_TYPE: "lsgan"

DATA:
  BATCH_SIZE: 10
  TARGET_SR: 16000
  SEGMENT: 2.555
  USE_QUANTITY: 0.5
  VALID_SPLIT: 0.1
  WEIGHTED_SR:
    ENABLE: True
  LPF:
    MULTIFILTER: True

DEBUG: False

WANDB:
  ENABLE: True
  TAGS: ["16k", "Data_0.5", "MPD", "LS-GAN", "Deep"]